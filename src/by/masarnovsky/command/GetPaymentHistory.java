package by.masarnovsky.command;

import javax.servlet.http.HttpServletRequest;

public class GetPaymentHistory implements ActionCommand {
    @Override
    public String execute(HttpServletRequest req) {
        String page = null;

        return page;
    }
}
